% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot3D.R
\name{plot3D.plain}
\alias{plot3D.plain}
\alias{plot3D.cont}
\alias{plot3D.cltrs}
\title{Interactive 3D Point Clouds}
\usage{
plot3D.plain(
  X,
  col = "gray55",
  point.type = c("point", "sphere"),
  point.size = 3,
  sphere.radius = NULL,
  alpha = 1,
  highlight = NULL,
  highlight.style = list(),
  non.highlight.style = list(col = "gray80", alpha = 0.4),
  axes = FALSE,
  xlab = "",
  ylab = "",
  zlab = "",
  aspect = c("equal", "normalized"),
  camera = list(),
  width = NULL,
  height = 600L,
  background.color = "white",
  layers = list(),
  shiny.brush = NULL
)

plot3D.cont(
  X,
  values,
  scale = NULL,
  legend.show = TRUE,
  legend.title = "Value",
  legend.position = c("left", "right"),
  legend.font.size = 12,
  legend.width = 240,
  ...
)

plot3D.cltrs(
  X,
  groups,
  scale = NULL,
  legend.show = TRUE,
  legend.title = "Group",
  legend.position = c("left", "right"),
  legend.font.size = 12,
  legend.width = 240,
  ...
)
}
\arguments{
\item{X}{Numeric matrix or all-numeric data frame with exactly three columns
and at least one row. Coordinates must be finite; rows are never dropped.}

\item{col}{Plain point colors, length one or nrow(X).}

\item{point.type}{Draw screen-space points or data-space spheres.}

\item{point.size}{Positive point size in screen pixels.}

\item{sphere.radius}{Positive radius in data units. NULL uses 1 percent of
the largest coordinate span, with a minimum of 1e-8. Does not choose type.}

\item{alpha}{Opacity multiplier in \verb{[0, 1]}, preserving alpha in supplied colors.}

\item{highlight}{NULL (all), a logical mask, or one-based row indices.}

\item{highlight.style, non.highlight.style}{Named style overrides: point.type,
point.size, sphere.radius, col, alpha. Color vectors must align to all rows.
Highlighting changes styling, never the fitted color scale or row identity.}

\item{axes}{Show axes.}

\item{xlab, ylab, zlab}{Axis labels.}

\item{aspect}{Equal data-unit scales (default), or normalized axis lengths.
Normalization distorts relative distances when coordinate spans differ.}

\item{camera}{Named list of theta, phi, fov, zoom, or a 4-by-4 userMatrix.}

\item{width, height}{Widget dimensions in pixels; NULL width fills its container.}

\item{background.color}{Canvas background color.}

\item{layers}{List of layer3D specifications, evaluated before widget capture.}

\item{shiny.brush}{Optional rgl brush configuration passed as shinyBrush.}

\item{values}{Numeric values, one per row. Missing values use the scale's NA color.}

\item{scale}{Reusable scale, or NULL to fit a default scale to all values/groups.}

\item{legend.show}{Show the HTML color legend.}

\item{legend.title}{Legend title.}

\item{legend.position}{Side of the scene for the legend.}

\item{legend.font.size}{Legend font size in pixels.}

\item{legend.width}{Legend maximum width in pixels, constrained by the container.}

\item{...}{Named scene controls from plot3D.plain, excluding X and col.
Unknown names and legacy argument spellings are rejected.}

\item{groups}{Group labels or factor, one per row; groups need not be clusters.}
}
\value{
An rglwidget/htmlwidget. \code{attr(widget, "ivue")} contains coordinates,
row.ids, mapped colors, highlight, draw.ids (row, object, index), camera,
aspect, captured scene, and (for colored plots) mapping data. Object IDs
describe the captured scene, not an open device. Save separately with
\code{htmlwidgets::saveWidget()}.
Mapped colors describe the base scale before highlight and opacity
overrides. Legends reflect the scale and global alpha, not highlight styles.
}
\description{
These functions always return a browser widget. They use a private null
device and restore caller graphics options and the previous device. Loading
ivue does not load rgl. No XQuartz or native display is required.
}
\examples{
set.seed(1)
xs <- runif(250, -1, 1)
ys <- runif(250, -1, 1)
X <- cbind(xs, ys, 1.2 * (xs^2 - ys^2))
if (nzchar(system.file(package = "rgl"))) w <- plot3D.plain(X, axes = TRUE)
if (nzchar(system.file(package = "rgl"))) {
  sc <- color.scale.cont(X[, 3])
  continuous <- plot3D.cont(X, X[, 3], scale = sc)
  grouped <- plot3D.cltrs(X, ifelse(X[, 3] >= 0, "positive", "negative"))
}
}
