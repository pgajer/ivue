% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{prepare.graph}
\alias{prepare.graph}
\title{Prepare Graph Data for Visualization}
\usage{
prepare.graph(graph, vertices = NULL, directed = NULL, weight.type = NULL)
}
\arguments{
\item{graph}{A named list with adj.list and weight.list (neighbors are row
indices); a list with edges and vertices; a weighted data frame with from,
to, weight columns plus the vertices argument; a numerical square adjacency
matrix; a Matrix sparse adjacency matrix; or an igraph object. Matrices use
zero for absent edges; use lists/tables to represent zero-weight edges.
A graph returned by \code{\link[=prepare.graph]{prepare.graph()}} can be reused directly.}

\item{vertices}{Explicit vertex IDs or a data frame with a unique id column.
Required for edge-table input, including isolated vertices. Adjacency lists
default to list names, or character row numbers when unnamed.}

\item{directed}{Logical directedness; NULL uses stored directedness or FALSE.
This release rejects directed rendering, self-loops, and parallel edges.
Undirected adjacency lists must be reciprocal with equal weights.}

\item{weight.type}{"distance", "strength", or "unweighted". Required for
weighted layout algorithms but not supplied-coordinate drawing. The fr
algorithm requires strengths; kk requires distances. No inversion occurs.
Unweighted mode only accepts missing or unit weights.}
}
\value{
An \code{ivue_graph} list with \code{vertices} (a data frame with canonical
character \code{id} plus supplied attributes), \code{edges} (a data frame with
integer \code{from} and \code{to} row indices into vertices, numerical \code{weight},
and supplied attributes), \code{directed}, and \code{weight.type}.
}
\description{
Validate graph input and expose vertex IDs, edge order, weights, and
attributes without loading rgl, opening a device, or computing a layout.
The prepared object can be inspected and reused by \code{\link[=plot3D.graph]{plot3D.graph()}}.
}
\details{
Vertex order is preserved. Edge-table input retains edge row order;
reciprocal undirected adjacency input retains one copy of each edge, from
the lower vertex index. Matrix inputs follow that adjacency convention.
\code{edge.col} and \code{edge.width} follow this prepared edge order and are never
inferred automatically from weights. Isolates remain in the vertex table.
Prepared objects are revalidated on reuse; editing them does not bypass
validation. Directed data can be prepared but cannot currently be rendered.
Missing weights require explicit \code{weight.type = "unweighted"}.
}
\examples{
edges <- data.frame(from = "a", to = "b", weight = 2)
graph <- prepare.graph(edges, vertices = c("a", "b", "isolate"),
                       weight.type = "strength")
graph$vertices
graph$edges
widths <- 1 + graph$edges$weight
X <- rbind(b = c(1, 0, 0), isolate = c(0, 1, 0), a = c(0, 0, 0))
values <- c(isolate = 3, a = 1, b = 2)
if (nzchar(system.file(package = "rgl"))) {
  w <- plot3D.graph(graph, X = X, values = values, edge.width = widths)
}
}
